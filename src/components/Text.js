import React from 'react'

const Text=({message}) =>{
  return (
    <div> 
        {/* Day2 task-2:-. Create a “Text” Component which accepts a message(string) as props, and display it */}
      <h3>{message}</h3>
    </div>
  )
}
export default Text
